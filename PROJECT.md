아래는 **이미지 데이터셋에 대한 마스킹 데이터 생성 도구** 개발을 위한 기획 및 플래닝 설계안입니다.
전제는 **작업자가 이미지를 보면서 브러시로 마스크 영역을 직접 추가/제거하고, 결과로 원본 이미지와 마스크 데이터를 함께 저장/관리하는 어노테이션 툴**입니다.

---

# 1. 프로젝트 개요

## 1.1 목적

이미지 데이터셋에서 특정 영역을 작업자가 직접 지정하여 **마스킹 데이터**를 생성하는 도구를 개발한다.

작업자는 이미지 위에 브러시를 사용해 마스크 영역을 칠하거나 지울 수 있으며, 생성된 마스크는 원본 이미지와 매칭되어 학습용 데이터셋으로 저장된다.

## 1.2 핵심 목표

| 구분     | 내용                              |
| ------ | ------------------------------- |
| 입력     | 이미지 데이터                         |
| 작업 방식  | 작업자가 브러시로 마스크 영역 직접 추가/제거       |
| 출력     | 원본 이미지 + 마스킹 데이터                |
| 주요 사용자 | 데이터 작업자, 검수자, 관리자               |
| 핵심 가치  | 빠른 작업 속도, 정확한 마스크 생성, 데이터 품질 관리 |

---

# 2. 사용자 역할 정의

## 2.1 작업자

이미지를 열고 브러시로 마스크를 생성한다.

주요 기능:

* 이미지 확인
* 브러시로 마스크 추가
* 지우개 또는 제거 브러시로 마스크 삭제
* 확대/축소
* 이전/다음 이미지 이동
* 작업 저장
* 작업 완료 처리

## 2.2 검수자

작업자가 만든 마스크 품질을 확인한다.

주요 기능:

* 원본/마스크 오버레이 확인
* 마스크 수정
* 승인 / 반려
* 반려 사유 입력
* 품질 기준 체크

## 2.3 관리자

프로젝트와 데이터셋을 관리한다.

주요 기능:

* 이미지 데이터셋 업로드
* 작업자 배정
* 진행률 확인
* 결과 데이터 다운로드
* 작업 이력 확인

---

# 3. 전체 작업 프로세스

```text
이미지 데이터 업로드
        ↓
프로젝트 생성
        ↓
작업자에게 이미지 할당
        ↓
작업자가 브러시로 마스크 추가/제거
        ↓
자동 저장 또는 수동 저장
        ↓
작업 완료 처리
        ↓
검수자 확인
        ↓
승인 또는 반려
        ↓
마스크 데이터 export
```

---

# 4. 주요 기능 설계

## 4.1 이미지 입력 기능

### 요구사항

이미지 데이터셋을 프로젝트 단위로 업로드할 수 있어야 한다.

지원 포맷:

* JPG
* JPEG
* PNG
* BMP
* WEBP
* 필요 시 TIFF 확장 가능

### 기능 상세

| 기능        | 설명                       |
| --------- | ------------------------ |
| 이미지 업로드   | 단일 이미지 또는 폴더/ZIP 업로드     |
| 이미지 목록 생성 | 업로드된 이미지를 리스트화           |
| 원본 보존     | 원본 이미지는 수정하지 않고 별도 저장    |
| 해상도 유지    | 마스크는 원본 이미지와 동일한 해상도로 생성 |
| 이미지 ID 부여 | 각 이미지에 고유 ID 생성          |

---

## 4.2 마스킹 편집 기능

## 핵심 편집 방식

작업자는 이미지 위에 마스크 레이어를 보면서 브러시로 영역을 직접 수정한다.

```text
원본 이미지 레이어
        +
마스크 오버레이 레이어
        +
브러시 편집 레이어
```

## 마스크 조작 모드

| 모드    | 설명                   |
| ----- | -------------------- |
| 추가 모드 | 브러시로 칠한 영역을 마스크에 추가  |
| 제거 모드 | 브러시로 칠한 영역을 마스크에서 제거 |
| 보기 모드 | 편집 없이 이미지 확인         |
| 이동 모드 | 이미지를 드래그하여 이동        |
| 확대 모드 | 특정 영역 확대 확인          |

---

# 5. 브러시 기능 상세 기획

## 5.1 기본 브러시 기능

| 기능        | 설명                                    |
| --------- | ------------------------------------- |
| 브러시 크기 조절 | 작은 영역부터 큰 영역까지 조정 가능                  |
| 브러시 하드니스  | 가장자리 부드러움 조절. 단, 최종 마스크는 binary 처리 가능 |
| 원형 브러시    | 기본 브러시 형태                             |
| 사각 브러시    | 필요 시 옵션으로 제공                          |
| 마스크 추가    | 선택 영역을 마스크 값 1 또는 255로 설정             |
| 마스크 제거    | 선택 영역을 마스크 값 0으로 설정                   |
| 커서 미리보기   | 현재 브러시 크기를 화면에 표시                     |

## 5.2 고급 브러시 기능

MVP 이후 확장 기능으로 고려할 수 있다.

| 기능         | 설명                              |
| ---------- | ------------------------------- |
| 경계 보정 브러시  | 가장자리 정리를 쉽게 하기 위한 작은 브러시        |
| 직선 브러시     | Shift 키를 누른 상태로 직선 마스킹          |
| 다각형 채우기    | 사용자가 점을 찍고 내부 영역 자동 채우기         |
| Flood Fill | 클릭한 영역과 유사한 색 영역을 자동 마스킹        |
| Edge Snap  | 객체 경계에 브러시가 붙도록 보조              |
| AI 보조 마스크  | 클릭 또는 박스 기준으로 자동 마스크 생성 후 수동 수정 |

---

# 6. 마스크 출력 데이터 설계

## 6.1 기본 출력 구조

권장 출력 구조는 다음과 같다.

```text
dataset/
 ├─ images/
 │   ├─ image_0001.png
 │   ├─ image_0002.png
 │   └─ image_0003.png
 │
 ├─ masks/
 │   ├─ image_0001_mask.png
 │   ├─ image_0002_mask.png
 │   └─ image_0003_mask.png
 │
 └─ metadata/
     └─ annotations.json
```

## 6.2 마스크 이미지 포맷

기본은 **8-bit grayscale PNG**를 권장한다.

| 값   | 의미          |
| --- | ----------- |
| 0   | 배경 / 마스크 없음 |
| 255 | 마스크 영역      |

예시:

```text
원본 이미지: image_0001.png
마스크 이미지: image_0001_mask.png
```

마스크는 반드시 원본 이미지와 동일한 width, height를 가져야 한다.

## 6.3 다중 클래스 확장

향후 여러 종류의 마스크가 필요한 경우 indexed mask를 사용할 수 있다.

| 픽셀 값 | 의미    |
| ---- | ----- |
| 0    | 배경    |
| 1    | 클래스 A |
| 2    | 클래스 B |
| 3    | 클래스 C |

MVP에서는 단일 마스크를 기준으로 설계하고, 추후 multi-class mask를 확장하는 방식이 좋다.

---

# 7. 메타데이터 설계

## 7.1 annotations.json 예시

```json
{
  "project_id": "mask_project_001",
  "version": "1.0.0",
  "created_at": "2026-04-29T00:00:00+09:00",
  "items": [
    {
      "image_id": "image_0001",
      "image_path": "images/image_0001.png",
      "mask_path": "masks/image_0001_mask.png",
      "width": 1920,
      "height": 1080,
      "status": "approved",
      "worker_id": "worker_01",
      "reviewer_id": "reviewer_01",
      "updated_at": "2026-04-29T00:00:00+09:00"
    }
  ]
}
```

## 7.2 상태값 정의

| 상태          | 설명         |
| ----------- | ---------- |
| not_started | 작업 전       |
| in_progress | 작업 중       |
| submitted   | 작업자가 완료 제출 |
| approved    | 검수 승인      |
| rejected    | 검수 반려      |
| revised     | 반려 후 수정 완료 |

---

# 8. UI 화면 설계

## 8.1 기본 레이아웃

```text
┌──────────────────────────────────────────────────────┐
│ 상단 메뉴: 프로젝트명 / 저장 / 제출 / 다운로드        │
├───────────────┬───────────────────────┬──────────────┤
│ 이미지 목록   │      작업 캔버스        │ 도구 패널     │
│               │                       │              │
│ image_001     │   원본 이미지          │ 브러시 크기   │
│ image_002     │   + 마스크 오버레이    │ 투명도        │
│ image_003     │                       │ 추가/제거     │
│               │                       │ 단축키 안내   │
├───────────────┴───────────────────────┴──────────────┤
│ 하단 상태바: 현재 이미지 / 작업 상태 / 저장 상태      │
└──────────────────────────────────────────────────────┘
```

## 8.2 화면 구성 요소

| 영역     | 구성                          |
| ------ | --------------------------- |
| 상단 메뉴  | 프로젝트명, 저장, 제출, 되돌리기, 내보내기   |
| 좌측 패널  | 이미지 목록, 작업 상태, 필터           |
| 중앙 캔버스 | 이미지 표시, 마스크 오버레이, 브러시 편집    |
| 우측 패널  | 브러시 설정, 마스크 설정, 단축키 안내      |
| 하단 상태바 | 저장 상태, 확대 비율, 좌표, 현재 이미지 번호 |

---

# 9. 편의 기능 기획

## 9.1 단축키

작업 속도 향상을 위해 단축키는 필수 기능으로 설계한다.

| 단축키               | 기능                          |
| ----------------- | --------------------------- |
| `B`               | 브러시 모드                      |
| `E`               | 지우개 / 마스크 제거 모드             |
| `V`               | 보기 모드                       |
| `Space + Drag`    | 화면 이동                       |
| `Ctrl + Z`        | 실행 취소                       |
| `Ctrl + Y`        | 다시 실행                       |
| `[`               | 브러시 크기 감소                   |
| `]`               | 브러시 크기 증가                   |
| `+`               | 확대                          |
| `-`               | 축소                          |
| `0`               | 화면 맞춤                       |
| `1`               | 원본 보기                       |
| `2`               | 마스크 오버레이 보기                 |
| `3`               | 마스크만 보기                     |
| `M`               | 마스크 표시/숨김                   |
| `S` 또는 `Ctrl + S` | 저장                          |
| `N`               | 다음 이미지                      |
| `P`               | 이전 이미지                      |
| `Enter`           | 작업 완료 제출                    |
| `Esc`             | 현재 동작 취소                    |
| `Shift + Drag`    | 직선 브러시                      |
| `Alt + Click`     | 해당 위치 마스크 제거 또는 색상 선택 기능 확장 |

## 9.2 단축키 커스터마이징

고급 기능으로 사용자가 단축키를 직접 변경할 수 있도록 한다.

기능:

* 단축키 설정 화면
* 중복 단축키 감지
* 기본값 복원
* 사용자별 단축키 저장

---

# 10. 작업 효율 향상 기능

## 10.1 자동 저장

작업 중 데이터 손실을 막기 위해 자동 저장을 제공한다.

권장 정책:

| 항목       | 내용                        |
| -------- | ------------------------- |
| 자동 저장 주기 | 5~10초                     |
| 저장 조건    | 마스크 변경 발생 시               |
| 저장 대상    | 현재 마스크, 작업 상태, undo 이력 일부 |
| 상태 표시    | “저장됨”, “저장 중”, “저장 실패” 표시 |

## 10.2 Undo / Redo

브러시 작업은 실수가 잦으므로 되돌리기 기능이 중요하다.

요구사항:

* 최소 30단계 이상 undo 지원
* 브러시 stroke 단위로 되돌리기
* 이미지 이동/확대는 undo 대상에서 제외
* 마스크 변경 작업만 undo 대상으로 관리

## 10.3 오버레이 투명도 조절

작업자가 원본과 마스크를 동시에 확인할 수 있도록 한다.

| 기능        | 설명                 |
| --------- | ------------------ |
| 마스크 색상 변경 | 기본 빨간색, 필요 시 변경 가능 |
| 투명도 조절    | 0~100%             |
| 마스크만 보기   | 원본 숨기고 마스크만 표시     |
| 원본만 보기    | 마스크 숨김             |

## 10.4 이미지 탐색 기능

| 기능       | 설명                    |
| -------- | --------------------- |
| 이전/다음 이동 | 단축키로 빠르게 이동           |
| 작업 상태 필터 | 미작업, 작업 중, 완료, 반려만 보기 |
| 검색       | 이미지명 또는 ID 검색         |
| 썸네일 표시   | 좌측 목록에서 빠른 확인         |
| 진행률 표시   | 전체 대비 완료 수 표시         |

---

# 11. 검수 기능 설계

## 11.1 검수 플로우

```text
작업자 제출
   ↓
검수자 확인
   ↓
승인 또는 반려
   ↓
반려 시 작업자 재수정
   ↓
최종 승인
```

## 11.2 검수 체크리스트

| 항목         | 기준                      |
| ---------- | ----------------------- |
| 마스크 누락 여부  | 필요한 영역이 빠지지 않았는가        |
| 과도한 마스크 여부 | 불필요한 영역이 포함되지 않았는가      |
| 경계 품질      | 객체 경계가 지나치게 뭉개지지 않았는가   |
| 해상도 일치     | 원본과 마스크 크기가 동일한가        |
| 빈 마스크 여부   | 의도치 않게 빈 마스크로 저장되지 않았는가 |
| 파일명 매칭     | 원본과 마스크 파일명이 정확히 매칭되는가  |

## 11.3 반려 사유

반려 시 사유를 선택 또는 입력할 수 있도록 한다.

예시:

* 마스크 영역 누락
* 마스크 영역 과다
* 경계 품질 미흡
* 잘못된 이미지 작업
* 기타 직접 입력

---

# 12. 데이터 품질 관리

## 12.1 자동 검증 항목

저장 또는 export 시 아래 항목을 자동 검사한다.

| 검증 항목         | 설명                              |
| ------------- | ------------------------------- |
| 원본-마스크 해상도 일치 | width, height 동일 여부             |
| 마스크 값 유효성     | 0/255 또는 지정된 class index만 존재하는지 |
| 파일 존재 여부      | 원본과 마스크 파일 모두 존재하는지             |
| 빈 마스크 감지      | 전체 값이 0인지 확인                    |
| 과도한 마스크 감지    | 전체 이미지 대비 마스크 비율이 너무 큰지 확인      |
| 손상 이미지 감지     | 로딩 불가 이미지 여부 확인                 |

## 12.2 품질 지표

관리자 화면에서 확인 가능한 지표:

| 지표       | 설명                  |
| -------- | ------------------- |
| 전체 이미지 수 | 프로젝트 내 전체 이미지       |
| 작업 완료 수  | 작업자가 제출한 이미지 수      |
| 승인 수     | 검수 통과 이미지 수         |
| 반려 수     | 검수 반려 이미지 수         |
| 평균 작업 시간 | 이미지 1장당 평균 작업 시간    |
| 작업자별 생산량 | 작업자별 완료 수           |
| 반려율      | 작업자별 또는 프로젝트별 반려 비율 |

---

# 13. Export 기능

## 13.1 Export 포맷

기본 export는 다음 세 가지를 권장한다.

| 포맷            | 설명                      | 우선순위 |
| ------------- | ----------------------- | ---- |
| PNG mask      | 이미지별 마스크 파일             | 높음   |
| JSON metadata | 이미지-마스크 매핑 정보           | 높음   |
| ZIP archive   | 전체 데이터셋 압축 다운로드         | 높음   |
| COCO format   | segmentation dataset 호환 | 중간   |
| RLE format    | 대용량 마스크 압축 표현           | 중간   |

## 13.2 Export 결과 예시

```text
export_mask_project_001.zip
 ├─ images/
 │   ├─ image_0001.png
 │   └─ image_0002.png
 │
 ├─ masks/
 │   ├─ image_0001_mask.png
 │   └─ image_0002_mask.png
 │
 ├─ annotations.json
 └─ export_summary.json
```

## 13.3 export_summary.json 예시

```json
{
  "project_id": "mask_project_001",
  "total_images": 1000,
  "exported_images": 980,
  "approved_images": 980,
  "excluded_images": 20,
  "exported_at": "2026-04-29T00:00:00+09:00"
}
```

---

# 14. 기능 우선순위

## 14.1 MVP 필수 기능

| 기능                 | 우선순위 |
| ------------------ | ---- |
| 이미지 업로드            | 필수   |
| 이미지 목록 표시          | 필수   |
| 캔버스 이미지 표시         | 필수   |
| 브러시 마스크 추가         | 필수   |
| 브러시 마스크 제거         | 필수   |
| 브러시 크기 조절          | 필수   |
| 확대/축소/이동           | 필수   |
| 마스크 오버레이 표시        | 필수   |
| 저장                 | 필수   |
| 자동 저장              | 필수   |
| Undo/Redo          | 필수   |
| 이전/다음 이미지 이동       | 필수   |
| PNG 마스크 출력         | 필수   |
| 원본-마스크 매핑 metadata | 필수   |

## 14.2 2차 개발 기능

| 기능          | 우선순위  |
| ----------- | ----- |
| 검수자 승인/반려   | 높음    |
| 작업자 배정      | 높음    |
| 작업 진행률 대시보드 | 높음    |
| 단축키 커스터마이징  | 중간    |
| 다중 클래스 마스크  | 중간    |
| COCO export | 중간    |
| Flood Fill  | 중간    |
| AI 보조 마스킹   | 낮음~중간 |

---

# 15. 기술 구조 설계

## 15.1 전체 구조

```text
Frontend
 - 이미지 캔버스
 - 브러시 편집
 - 단축키 처리
 - 마스크 오버레이
 - 작업 상태 표시

Backend
 - 프로젝트 관리
 - 이미지 파일 관리
 - 마스크 저장
 - 사용자/권한 관리
 - export 처리

Storage
 - 원본 이미지
 - 마스크 이미지
 - 메타데이터
 - 작업 이력

Database
 - 프로젝트 정보
 - 이미지 정보
 - 작업 상태
 - 사용자 정보
 - 검수 이력
```

## 15.2 프론트엔드 주요 모듈

| 모듈                 | 설명            |
| ------------------ | ------------- |
| Canvas Viewer      | 이미지 렌더링       |
| Mask Editor        | 브러시 추가/제거 처리  |
| Shortcut Manager   | 단축키 관리        |
| History Manager    | Undo/Redo 관리  |
| Autosave Manager   | 자동 저장 처리      |
| Image Navigator    | 이미지 목록 및 이동   |
| Overlay Controller | 마스크 색상/투명도 제어 |

## 15.3 백엔드 주요 모듈

| 모듈          | 설명            |
| ----------- | ------------- |
| Project API | 프로젝트 생성/조회/수정 |
| Image API   | 이미지 업로드/목록 조회 |
| Mask API    | 마스크 저장/불러오기   |
| Review API  | 승인/반려 처리      |
| Export API  | 결과 데이터 다운로드   |
| Auth API    | 사용자 인증/권한 관리  |

---

# 16. API 예시 설계

## 16.1 프로젝트 생성

```http
POST /api/projects
```

Request:

```json
{
  "name": "mask_dataset_project",
  "description": "이미지 마스킹 데이터 생성 프로젝트"
}
```

## 16.2 이미지 목록 조회

```http
GET /api/projects/{project_id}/images
```

Response:

```json
{
  "items": [
    {
      "image_id": "image_0001",
      "file_name": "image_0001.png",
      "status": "in_progress",
      "width": 1920,
      "height": 1080
    }
  ]
}
```

## 16.3 마스크 저장

```http
PUT /api/images/{image_id}/mask
```

저장 방식은 두 가지 중 하나를 선택할 수 있다.

### 방식 A: 마스크 PNG 직접 업로드

```text
multipart/form-data
- mask_file: image_0001_mask.png
- status: in_progress
```

### 방식 B: Base64 또는 binary blob 전송

```json
{
  "mask_data": "base64_encoded_png",
  "status": "in_progress"
}
```

실무에서는 대용량 이미지 처리와 성능을 고려해 **multipart 업로드 방식**을 권장한다.

---

# 17. 데이터베이스 테이블 설계 예시

## 17.1 projects

| 컬럼          | 타입       | 설명      |
| ----------- | -------- | ------- |
| id          | string   | 프로젝트 ID |
| name        | string   | 프로젝트명   |
| description | text     | 설명      |
| created_by  | string   | 생성자     |
| created_at  | datetime | 생성일     |
| updated_at  | datetime | 수정일     |

## 17.2 images

| 컬럼          | 타입       | 설명        |
| ----------- | -------- | --------- |
| id          | string   | 이미지 ID    |
| project_id  | string   | 프로젝트 ID   |
| file_name   | string   | 원본 파일명    |
| image_path  | string   | 원본 저장 경로  |
| mask_path   | string   | 마스크 저장 경로 |
| width       | int      | 이미지 너비    |
| height      | int      | 이미지 높이    |
| status      | string   | 작업 상태     |
| assigned_to | string   | 작업자 ID    |
| created_at  | datetime | 등록일       |
| updated_at  | datetime | 수정일       |

## 17.3 reviews

| 컬럼          | 타입       | 설명                  |
| ----------- | -------- | ------------------- |
| id          | string   | 검수 ID               |
| image_id    | string   | 이미지 ID              |
| reviewer_id | string   | 검수자 ID              |
| result      | string   | approved / rejected |
| comment     | text     | 검수 코멘트              |
| created_at  | datetime | 검수일                 |

## 17.4 mask_versions

| 컬럼         | 타입       | 설명        |
| ---------- | -------- | --------- |
| id         | string   | 버전 ID     |
| image_id   | string   | 이미지 ID    |
| version    | int      | 마스크 버전    |
| mask_path  | string   | 마스크 파일 경로 |
| editor_id  | string   | 수정자       |
| created_at | datetime | 생성일       |

---

# 18. 개발 일정 예시

## Phase 1. MVP 개발

목표: 기본 이미지 마스킹 작업 가능

| 작업         | 내용                         |
| ---------- | -------------------------- |
| 프로젝트 구조 설계 | 프론트/백엔드/스토리지 구조 확정         |
| 이미지 업로드    | 이미지 등록 및 목록화               |
| 캔버스 뷰어     | 이미지 표시, 확대/축소/이동           |
| 브러시 편집     | 마스크 추가/제거                  |
| 마스크 저장     | PNG mask 저장                |
| 단축키        | 기본 단축키 적용                  |
| 자동 저장      | 변경 사항 자동 저장                |
| export     | images/masks/metadata 다운로드 |

## Phase 2. 검수 및 운영 기능

목표: 실제 데이터 작업 운영 가능

| 작업     | 내용                |
| ------ | ----------------- |
| 사용자 권한 | 작업자/검수자/관리자 구분    |
| 작업 배정  | 이미지별 작업자 할당       |
| 검수 기능  | 승인/반려/수정          |
| 대시보드   | 진행률, 작업량, 반려율     |
| 품질 검증  | 빈 마스크, 해상도 불일치 감지 |
| 이력 관리  | 마스크 버전 관리         |

## Phase 3. 고도화

목표: 생산성과 품질 향상

| 작업          | 내용                   |
| ----------- | -------------------- |
| 단축키 커스터마이징  | 사용자별 단축키 설정          |
| 다중 클래스      | 여러 종류의 마스크 지원        |
| COCO export | 외부 학습 포맷 호환          |
| AI 보조       | 자동 마스크 생성 후 수동 보정    |
| 경계 보정       | Edge snap, smoothing |
| 협업 기능       | 코멘트, 작업 로그, 실시간 상태   |

---

# 19. 리스크 및 대응 방안

| 리스크            | 설명                 | 대응                              |
| -------------- | ------------------ | ------------------------------- |
| 대용량 이미지 렌더링 지연 | 고해상도 이미지에서 캔버스 느려짐 | 타일 렌더링, WebGL, 이미지 리사이즈 preview |
| 마스크 저장 실패      | 네트워크 또는 파일 저장 오류   | 자동 재시도, 로컬 임시 저장                |
| 작업 데이터 손실      | 브라우저 종료, 오류 발생     | 자동 저장, 변경 감지, 복구 기능             |
| 품질 편차          | 작업자마다 기준이 다름       | 가이드라인, 검수, 샘플 이미지 제공            |
| 단축키 충돌         | 브라우저 기본 단축키와 충돌    | 커스터마이징, 충돌 감지                   |
| 원본-마스크 불일치     | 파일명 또는 해상도 오류      | export 전 자동 검증                  |

---

# 20. MVP 기준 완료 조건

MVP는 아래 조건을 만족하면 1차 완성으로 볼 수 있다.

| 항목     | 완료 기준                              |
| ------ | ---------------------------------- |
| 이미지 로딩 | 업로드된 이미지를 캔버스에서 볼 수 있음             |
| 마스크 추가 | 브러시로 영역을 칠하면 마스크가 생성됨              |
| 마스크 제거 | 지우개 모드로 마스크 영역을 제거할 수 있음           |
| 저장     | 마스크가 원본과 동일 해상도의 PNG로 저장됨          |
| 불러오기   | 저장된 마스크를 다시 열어 수정 가능               |
| 단축키    | 브러시, 지우개, 저장, undo, 확대/축소 사용 가능    |
| 이동     | 이전/다음 이미지로 이동 가능                   |
| export | 원본 이미지, 마스크 이미지, metadata를 다운로드 가능 |
| 검증     | 원본과 마스크의 크기 불일치를 감지 가능             |

---

# 21. 권장 MVP 기능 목록

초기 개발 범위는 아래 정도로 잡는 것이 적절하다.

```text
1. 프로젝트 생성
2. 이미지 업로드
3. 이미지 목록 표시
4. 캔버스 이미지 뷰어
5. 브러시 추가 모드
6. 브러시 제거 모드
7. 브러시 크기 조절
8. 확대/축소/이동
9. 마스크 오버레이 투명도 조절
10. Undo / Redo
11. 자동 저장
12. 수동 저장
13. 이전/다음 이미지 이동
14. 작업 완료 상태 변경
15. PNG 마스크 export
16. annotations.json export
```

---

# 22. 기획 요약

이 도구의 핵심은 단순히 이미지를 칠하는 기능이 아니라, **데이터셋 생성 작업을 안정적으로 운영할 수 있는 마스킹 어노테이션 시스템**을 만드는 것이다.

초기 MVP에서는 다음에 집중하는 것이 좋다.

1. **브러시 기반 마스크 추가/제거**
2. **원본과 동일 해상도의 마스크 저장**
3. **작업 속도를 높이는 단축키**
4. **자동 저장과 Undo/Redo**
5. **원본 이미지와 마스크 데이터의 안정적인 export**

이후 검수, 작업자 배정, 품질 관리, AI 보조 마스킹 기능을 단계적으로 추가하면 실제 데이터 구축 업무에 사용할 수 있는 완성도 높은 시스템으로 확장할 수 있다.

